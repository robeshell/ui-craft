interface:
  display_name: "UI Craft"
  short_description: "任务驱动的 UI 设计、重排、精修与评审：先定主次，再分组、对齐、建立产品表达，最后以实际渲染验收"
  default_prompt: "使用 $ui-craft 处理当前页面。先判断用户在此页要完成什么，据此确定视觉主次，保留已有品牌和业务，调整信息分组与视觉细节，并以实际渲染验收。"
