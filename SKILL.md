---
name: ui-craft
description: 任务驱动的 UI 设计、重排、精修与评审方法，适用于网页和 iOS / Android / HarmonyOS 原生应用。只要用户提到界面、页面、布局、排版、视觉、配色、字体、间距、圆角、图标、组件样式、设计规范、DESIGN.md，或抱怨页面"像原型"、"不够精致"、"主次不清"、"太乱"、"有 AI 味"、"颜色太花"、"紫色渐变"，或要求生成、重排、美化、精修、评审任何带 UI 的页面和组件（包括 HTML/CSS、Tailwind、React、Vue、SwiftUI、Compose、ArkUI 代码），即使没有明说"设计"二字，也要使用本 skill。不用于纯业务逻辑、后端、数据处理或不影响界面呈现的修改。
---

# UI Craft

让界面的视觉权重跟着用户任务走，再用信息关系组织空间，最后把品牌和细节落到能验证的实现上。整齐只是及格线；精致来自层级、比例、字体、图形和状态细节同时成立。

方法基于 CRAP（亲密性、对齐、重复、对比）、平台官方规范和实际迭代经验，见 [来源与边界](references/sources.md)。日常任务不需要重读来源。

## 第 0 步：判断任务模式

先确定本次是哪一种任务，再决定做多少。做多了会把一个小改动变成未经请求的整套重构，做少了会把"重设计"做成换个颜色。

| 模式 | 典型请求 | 要走的步骤 | 交付物 |
| --- | --- | --- | --- |
| 局部修正 | "这个按钮对不齐"、"数字换行了"、"这块间距不对" | 直接定位根因，按第 4 步规则修，跳过 1 到 3 | 修改后的代码 + 一句根因说明 |
| 页面设计 / 重排 | "把这个页面做好看"、"重新排一下布局"、"像原型" | 1 → 2 → 3 → 4 → 5 全走 | 设计说明 + 代码或稿件 + 验收记录 |
| 规范整理 | "整理一份设计规范"、"统一多个页面的样式" | 2 → 3 → 4，产出规范表 | 规范文档，写明未验证渲染 |
| 评审 | "看看这个页面有什么问题" | 按第 5 步检查表逐项查 | 按影响排序的问题列表 |
| 无需改动 | 任何模式下，检查后没发现要改的 | 说明检查了什么、为什么不改 | 一句结论 + 检查项，不产生 diff |

开工前收集这些信息，能从代码、截图和项目文件里读到的就不要问用户：

- 用户在这个页面要完成什么任务，主要操作是什么。
- 真实内容与状态：真实的文字长度、数字位数、列表条数、空态和失败态。样例数据不能冒充真实状态。
- 已有约束：品牌色、字体、组件库、项目规范文件（DESIGN.md、AGENTS.md、CLAUDE.md、tokens、theme、团队自己的 DSL）。先把这些读完再动手，用户锁定的东西不改。
- 目标平台和观察尺度：Web 的目标视口宽度，或原生设备的逻辑尺寸。
- 可调整范围：只改样式，还是可以调整信息结构和文案。

只有当答案会改变方案、且现有证据无法判断时才提问。比如"这个页面的主任务是看实时状态还是做配置"值得问；"要不要用圆角"不值得问，按第 3 步自己判断。

### 两条硬规则

这两条优先于本 skill 里的其他所有规则。

**第一条：不为了优化而优化。** 读完页面后先判断它需不需要改。主体已经占主导、分组清楚、没有对比度或截断问题、状态齐全，那结论就是"不用改"，直接说出来，列出检查过哪些项，然后停下。这也是一种交付。不要为了显得做了事，去换圆角、调间距、改灰度，这些改动没有解决任何问题，只会制造 diff。用户问"还能不能更好"时，也可以回答"现在这样可以"，最多附一两条标为"可选"的建议，不动手。局部修正只改用户指出的那一处，顺手看到的其他问题写在回复里，不改。

**第二条：项目规范高于本 skill。** 约束的优先级从高到低是：用户这次的明确指令，项目自己的规范（DESIGN.md、AGENTS.md、CLAUDE.md、design tokens、主题文件、组件库、团队自己的 DSL），本 skill 的判定规则，本 skill 的临时基线。低一级永远不能推翻高一级。项目规范规定 4px 圆角，本 skill 建议 12px，那就用 4px。规范要求指标卡带颜色，本 skill 说不要彩虹卡，那就带颜色。遇到冲突可以在回复里提一句"规范里这条和常见做法不同"，但页面按规范做，规范文件本身不改。只有用户明确说了"可以不遵守规范"或"这次不用管 DESIGN.md"，才可以偏离，偏离了也要逐条写明打破了哪一条。

## 第 1 步：从任务推导视觉权重

先写一句话："用户在此页要完成 ____。"然后列出优先级表：

| 优先级 | 内容 | 视觉分配 |
| --- | --- | --- |
| P0 持续关注的信息 | 用户完成任务时一直要看的东西 | 最大面积、最强对比、视线首个落点 |
| P1 判断依据 | 做决定时需要对照的信息 | 紧邻 P0，面积次之 |
| P2 主要操作 | 完成任务的那一两个动作 | 固定位置、高对比、够大的热区 |
| P3 辅助操作与信息 | 设置、帮助、次要入口、说明 | 小面积、低对比、边缘位置 |

按页面类型判断主体是什么，不要全产品套一个比例：

| 页面类型 | 主体应该是 | 常见错误 |
| --- | --- | --- |
| 实时作业 / 监控 | 当前状态与反馈 | 控件面板挤掉状态显示 |
| 内容阅读 | 正文与阅读节奏 | 工具栏和推荐位抢走正文宽度 |
| 决策 / 比较 | 可对照的数据与行动按钮 | 数据分散在等大卡片里无法对照 |
| 表单 / 配置 | 输入区与提交 | 说明文字比输入框还醒目 |
| 列表 / 工单处理 | 待处理项本身 | 统计卡片占了首屏，列表要滚动才能看到 |

把优先级映射到面积、位置、对比度和留白时，记住几件事：

- 功能数量、控件数量和实现难度都不决定重要性。六个模块不等于六张等大的卡片。
- 注意力顺序不等于上下顺序。顶部放导航、底部固定主操作都可以，主体仍要在视觉上占主导。
- 压缩辅助区先去无效留白和重复表达，不压缩必要文字和热区。并列导致数值掉行、单位和数字分家、按钮挤不下，就改纵向排列；宽屏和比较任务可以合理并列。

检查：去掉所有颜色和装饰，只看面积和阅读顺序，主次是否仍然成立？用户能否在两秒内找到最重要的信息和下一步操作？

## 第 2 步：用信息关系组织空间

按"亲密性 → 对齐 → 重复 → 对比"的顺序处理。先归组，再考虑容器样式，因为容器是分组的结果，不是分组的手段。

**亲密性。** 相关的靠近，无关的拉开。标题靠近它统领的内容，离上一组更远。判定：

- 一组对象各自独立、可单独操作或收藏，用卡片或独立容器。
- 信息连续、结构相同、用户会逐行扫描，用列表行加细分隔或纯留白，不要每行一张卡。
- 工具和操作聚成工具区，不散落在内容里。

**对齐。** 建立少量连续对齐线：标题起线、正文起线、数值列、尾部操作列。判定：

- 长正文左对齐。
- 成对的概览指标可以组内居中，但同一行的多组要共享一条基线。
- 数字比较必须右对齐或按小数点对齐，且使用等宽数字，否则列会随着数值变化左右漂移。
- 标签和它旁边的读数要检查垂直关系，尤其是字号不同时，按基线对齐而不是按容器居中。

**重复。** 同一角色用同一套规则：字体、颜色、间距、图标、组件状态。不同功能不必为了整齐强制用相同容器。

**对比。** 真正的重点只需要一种明确差异。优先用面积、位置、字重或疏密，其次才是颜色。给一个元素同时加粗、变色、放大、加底块，等于告诉用户"我也不知道哪个重要"。

分区可以用五种手段组合表达：背景与内容表面的明度差、空间间隔、区块标题、连续的对齐线、弱分隔线。只靠给每块加描边会保留线框原型感；只把边框全部撤掉也不会自动产生层级。

## 第 3 步：建立产品表达

当任务包含视觉设计且方向未定时，从已有品牌和内容里挑两到三个能贯穿页面的视觉特征，并说清楚每个特征服务什么。例如：

- 精确、克制的作业工具：稳定的等宽数字、简洁的线性图形、清楚的状态色。
- 内容产品：封面图比例、图文节奏、正文字体的阅读感。
- 品牌感强的消费产品：品牌色的固定出现位置、插画风格、标志性圆角。

避免只写"高级、精致、现代"这类无法实施的形容词。写出来的特征必须能对应到具体属性。

装饰决策规则：

| 属性 | 用于 | 不要 |
| --- | --- | --- |
| 描边 | 输入框、需要明确边界的可操作控件 | 普通容器默认加深色描边 |
| 阴影 | 表达悬浮和遮挡：弹层、拖拽中的元素、贴顶栏 | 每张卡片默认带阴影 |
| 渐变 | 品牌区域、局部层次、图表填充 | 整页背景或所有按钮 |
| 圆角 | 按组件角色分档，嵌套时内小外大 | 按钮、头像、卡片、弹层全做成同一个药丸形 |
| 插画 / 图形 | 空态、引导、品牌区 | 为填空白放无意义插画 |
| 大面积着色 | 展示页首屏、选中态的浅色调 | 后台页头整条品牌色、每张指标卡一种粉彩底 |
| 图标底块 | 平级入口的宫格，一组同一样式 | 列表行、设置项、指标卡里每行一个彩色底块 |

大色块背景、每行一个彩色图标底块、渐变首屏，这三种表现的根因是同一个：想表达"这块很重要"，但只会用颜色。修法是重要性靠面积、位置和字重表达，颜色留给含义。给一块区域上色之前先问它在告诉用户什么，答案是"为了醒目"就换手段。颜色分层、颜色预算和 AI 味特征清单见 [层次配色](references/color-layers.md)。

用真实内容建立视觉丰富度：图表、地图、图片、数据和排版本身都可以是主体。不为填空白增加假数据、装饰插画或不存在的功能。

原生应用的状态栏、导航栏和内容区分开处理。按实际实现能力决定是否透明或延伸背景，不画出无法落地的穿透效果。

交付时要能指出相比上一版具体改善了什么：主体更突出、图形更统一、信息更容易识别、操作反馈更清楚。只换蓝色、加阴影、调圆角不算完成视觉重设计。

方向已经稳定的项目直接沿用。方向未定且用户要求探索时，用一个代表性区域比较两三个方案，不把所有任务都变成多方案展示。

## 第 4 步：把规则落到细节

处理文字、色彩、间距、图标、组件状态和平台单位时，读 [视觉决策规则](references/visual-rules.md)。它包含判定规则和一份标注为"临时基线"的起始值表，供项目没有规范时使用。页面底、表面、区块、图标这些层次上的颜色怎么分配，以及怎么避开大色块、图标底块、紫色渐变这类 AI 味，读 [层次配色](references/color-layers.md)。

最容易出错的几条：

- 文字按角色同时定义字号、字重、行高、颜色和内容增长行为。只有一串字号不是规范。
- 中文界面用真实中文内容验收，不用英文短词占位。中文常规正文不加字距。
- 频繁更新的数字用等宽数字，并给容器预留最大位数的宽度，避免更新时挤动旁边的元素。
- 颜色先定义用途再挑色值。普通文字对比度至少 4.5:1，按完整精度判定，4.49 不是 4.5。灰字要按它实际所在的底色算，白底达标的灰放到浅灰页面底上可能不达标。
- 图标的可见尺寸、控件表面和触控热区是三个不同的数。不能靠放大整块底板凑热区，也不能靠相邻热区重叠凑尺寸。
- 测量实际可见边距。透明热区、字形的内部留白、滚动条和嵌套内边距都会让"CSS 左右都是 16"在视觉上不等距。
- 先修容器和行高的根因，再做光学微调。逐项堆 `margin-top: -1px` 是在掩盖问题。
- 验证实际存在的按下、选中、禁用、加载、失败、焦点状态。动效解释状态和空间变化，不能延迟高频操作。

如果环境里装了 better-layout、better-typography、better-ui，可以按需用它们深入某一块。它们是补充，不是本 skill 的前提，也不覆盖项目规则。

## 第 5 步：以实际结果验收并交付

按 [检查与交付](references/review.md) 选择与改动相称的检查范围。核心原则：

- 以目标逻辑尺寸查看真实文字和交互。缩略拼图会让巨大的文字看起来很精致，单页放大才能看出问题。
- 代码检查、生成图和缩略图不能替代实际渲染。网页原型不能证明原生落地效果。
- 分别记录"规则已定义、代码已实现、渲染已检查、交互已验证、用户已接受"，不合并成一个"完成"。

**交付格式。** 页面设计或重排任务用这个结构：

```markdown
## 本页任务
用户在此页要完成：…

## 改了什么
- 主体：… → …（为什么）
- 分组：…
- 表达：…

## 实查记录
- 已在 [视口/设备] 查看：…
- 检查过的状态：…
- 未验证：…
```

判断为无需改动时，回复只有三部分：结论"不需要改"，检查过的项目，以及至多两条标为"可选"的建议；不改任何文件。局部修正只需要一句根因加修改，不附实查记录表。评审任务按影响排序：影响读取或操作的问题 → 跨页一致性问题 → 细节优化，每条写明位置、现象、影响和建议，没有证据的标为"待查"，末尾一句话说明未做渲染验证即可，同样不附实查记录表。规范整理任务写明这是文档交付，尚未验证渲染。

反馈来了先定位根因，再决定改组件、页面还是规范。只有能解释多个场景的经验才写进规范；单页特例留在项目里。

## 一个完整示例

客服工单处理页，原稿是六张等大的卡片：今日统计、待处理工单、我的工单、公告、快捷操作、最近动态。用户在这页要完成的任务是"处理下一条待办工单"。

按第 1 步排优先级：待处理工单列表是 P0，工单的优先级和等待时长是 P1，"接单"是 P2，统计、公告和最近动态都是 P3。于是待处理列表占据主体宽度和首屏高度，统计压缩成顶部一行数字，公告和动态收进右侧窄栏并降低对比，快捷操作合并进列表行尾。

按第 2 步，工单是连续、结构相同的对象，从卡片改成列表行，行内建立"编号 / 标题 / 等待时长 / 优先级 / 操作"五条对齐线，等待时长右对齐并用等宽数字。按第 3 步，保留项目原有的品牌蓝，只用于"接单"按钮和当前选中行，其余状态色按语义定义。

完整的前后对照、规范表和实查记录见 [示例](references/example.md)。遇到"所有模块等大"的页面时先读它。

## 工具

计算不透明色对的对比度：

```bash
python3 scripts/contrast.py '#222222' '#FFFFFF'
```

前景或背景带透明度时，用 `--over` 传入页面底色，脚本会先合成再计算：

```bash
python3 scripts/contrast.py 'rgba(0,0,0,0.6)' '#F5F5F5' --over '#FFFFFF'
```

脚本只回答一个色对是否达到 4.5:1 和 3:1。渐变、图片背景需要取文字实际覆盖区域里最不利的颜色来算。整个产品是否符合无障碍标准需要另行判断。

从品牌色生成一套带色调的中性灰阶，浅色和深色模式各一套，并算好每一档上文字的对比度：

```bash
python3 scripts/surfaces.py '#2563EB'
```

它输出页面底、内容表面、浮起层、遮罩层和三档文字的色值。文字色会自动加深到在最不利的表面上也过 4.5:1。
