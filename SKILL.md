---
name: ui-craft
description: 以用户任务和功能主次驱动网页与原生 App 的 UI 设计、重排、精修和规范整理。用于界面分区不清、主次平均、排版混乱或只有原型感等问题；将信息关系、品牌表达与实际渲染连接起来。不用于纯业务逻辑修改。
---

# UI Craft

先让视觉权重符合用户任务，再让信息关系清楚，最后形成统一且有辨识度的产品表达。整齐是基础；精致来自层级、比例、字体、图形与交互细节共同成立。

本方法融合 CRAP、教程研究和实际迭代经验，是独立整理的工作方法，不宣称这些基础原则为原创。[来源与边界](references/sources.md)用于追溯，不要求每次设计重读资料。

## 判断本次要解决什么

读取用户要求、现有页面、品牌资产和项目规范（如 DESIGN.md）。区分交付规范、设计、代码还是评审。局部修正直接处理相关问题，不强制重做调研、整套设计系统或额外审批。

确定核心任务、真实内容与状态、可调整范围、目标平台和观察尺度。保留用户锁定的品牌与已有业务；样例数据不能冒充真实状态。只询问会改变方案且无法从现有证据判断的信息。

## 1. 从任务推导视觉权重

先写一句“用户在此页要完成什么”，再排出持续关注的信息、判断依据、主要操作和辅助操作的优先级。将它映射到面积、位置、对比度和留白。

- 功能数量、控件数量和实现复杂度不决定重要性，不把所有模块做成等大、同款、同等强调的卡片。
- 实时作业页可能以反馈为主，内容页以阅读为主，决策页以比较与行动为主。按当前任务判断，不能全产品套用某一种页面比例。
- 注意力顺序不等于机械的上下顺序。顶部可以放必要导航，底部可以固定主要操作，主体仍应在视觉上占主导。
- 缩减辅助区域先去无效留白和重复表达，不压缩必要文字、操作范围或信息。并列造成数值掉行、单位分离或操作困难时，改为合适的纵向排列；宽屏和比较任务也可以合理并列。

检查：用户能否迅速找到本页最重要的信息，以及下一步操作？去掉装饰后，面积和阅读顺序是否仍体现主次？

## 2. 用信息关系组织空间

按“亲密性 → 对齐 → 重复 → 对比”处理 CRAP，把相关信息归组后再考虑容器样式。

- **亲密性**：组内更近，组间更远；标题靠近它统领的内容。独立对象、连续列表、工具区应按关系选择不同组织方式。
- **对齐**：建立明确的文字起线、数值列、控件中心线。标签与相邻读数检查垂直关系；数字比较需要稳定对齐。长正文通常左对齐，成对概览指标可以组内居中，不要求所有内容都居中。
- **重复**：同义内容采用同一角色规则，复用字体、颜色、间距、图标和组件状态。不同功能不必为了整齐强制使用相同容器。
- **对比**：真正的重点应有明确差异；优先调整比例、位置、字重或疏密，不给每项同时加粗、变色、放大和加底块。

分块可以通过背景与内容表面、空间、标题、连续对齐线和弱分隔共同表达。只给每块加描边容易保留线框原型感；只撤掉边框也不会自动形成层级。

## 3. 建立产品表达

当任务包含视觉设计、且方向还未确定时，从已有品牌和内容选择少量能贯穿页面的视觉特征，并说明它们如何服务产品。例如精确、克制的作业工具可用稳定数字、简洁图形和清楚状态表达；内容产品可能更多依靠封面、图文比例和阅读节奏。避免只写“高级、精致”等无法实施的形容词。

- 延续品牌的辨识特征，装饰集中在适合的区域。品牌插图、导航图标与数据图形可承担不同角色，但各自保持一致。
- 用真实内容建立视觉丰富度：图表、地图、图片、数据和排版本身都能成为主体。不为填空白增加假数据、无意义插画或不存在的功能。
- 背景、圆角、渐变和阴影服从产品气质与空间关系。普通容器不默认深描边；阴影表达悬浮，渐变服务品牌或局部层次。也不把白卡浅灰底、无边框或小圆角规定为所有项目唯一风格。
- 原生状态栏、应用导航和内容区分别处理。按实际实现能力决定是否透明或延伸背景，不能只为截图效果画出无法落地的穿透。
- 相比已认可版本，应能指出具体改善了什么：主体更突出、图形更统一、信息更容易识别或操作反馈更清楚。仅换蓝色、加阴影、调圆角，不足以说明完成了视觉重设计。

有稳定方向时直接沿用。方向尚未确定且用户要求探索时，可以用代表性区域比较方案，不强制把所有任务变成多方案展示。

## 4. 将规则落到细节

需要整理文字、色彩、组件或平台尺度时读 [视觉决策规则](references/visual-rules.md)。

- 文字按角色同时定义字号、字重、行高、颜色和内容增长行为。检查中文、数字和单位混排；动态读数避免更新挤动布局。
- 颜色先定义品牌、操作、内容、辅助、表面和状态用途。使用真实合成背景检查可读性；修改品牌要服从项目要求，不能暗中换色。
- 区分图标可见尺寸、控件表面与操作热区，避免为了可点击把底板全部放大，也不能靠热区重叠凑尺寸。
- 圆角按角色与嵌套关系协调，图标按有效面积和重心校正。先修容器与行高根因，再做必要的光学微调，避免逐项堆偏移补丁。
- 测量实际可见边距：透明热区、字形、滚动条和嵌套内边距都会影响结果。CSS 左右数值一样，不证明视觉边距一样。
- 验证实际存在的按下、选中、禁用、加载、失败和焦点等状态。动效解释状态与空间变化，不能延迟高频操作或伪造实时反馈。

若环境已提供，按需使用 better-layout、better-typography、better-ui 深入布局、文字与控件精修。它们是补充，不覆盖项目规则，也不是本 skill 可用的前提；不自动安装更多技能或叠加所有配方。

## 5. 以实际结果验收并沉淀

按 [检查与交付](references/review.md)选择与改动相称的检查。以目标逻辑尺寸查看真实文字与交互，检查相关小屏、大字、长内容及滚动状态。代码检查、生成图和缩略拼图不能替代实际渲染；网页原型也不能证明原生落地效果。

规范记录应区分：通用判断方法、用户明确要求、项目工作基线、当前样板取值、未验证假设。项目颜色、尺寸、特定页面顺序存入项目规范，不复制为本 skill 的通用限制。

反馈先定位根因，再决定修改组件、页面还是规范。只有能解释多个场景的经验才提升为通用方法；单页特例留在项目。更新现行规则对应条目，把旧决定放入历史，避免在文末不断追加相反要求。

交付说明改变了什么、解决什么问题、实查了哪些结果，以及尚未覆盖什么。规则定义、代码实现、渲染检查、业务验证与用户接受分别记录。文档任务不宣称新页面已验收。

需要计算不透明 sRGB 色对时，可在本 skill 目录运行：

```bash
python3 scripts/contrast.py '#222222' '#FFFFFF'
```

该脚本仅计算色对；渐变、图片或透明叠色先取得实际合成背景。整个产品是否符合适用标准需另行判断。
